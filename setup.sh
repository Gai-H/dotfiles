#!/bin/bash

mkdir -p ~/.config

# Karabiner-Elements
ln -s $(pwd)/karabiner ~/.config

# AeroSpace
ln -s $(pwd)/aerospace ~/.config

# Kitty
ln -s $(pwd)/kitty ~/.config

# Border
ln -s $(pwd)/borders ~/.config
